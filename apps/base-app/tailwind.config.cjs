/** @type {import("tailwindcss").Config} */
module.exports = {
  presets: [require('@deur/tailwind-config')],
  theme: {
    extend: {
      fontFamily: {
        display: [''],
        body: [''],
        text: [''],
      },
    },
  },
};
